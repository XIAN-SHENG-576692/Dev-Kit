# For compile typst
build-essential

# Fix curl: (77) error setting certificate file
ca-certificates

# For compile typst
libssl-dev

# For compile typst
pkg-config

# For install latest rust
rustup

# For unzip fonts.zip from anywhere
unzip
